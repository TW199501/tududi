interface:
  display_name: "Tududi"
  short_description: "Repo-specific patterns and workflows for tududi"
  default_prompt: "Use the tududi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true